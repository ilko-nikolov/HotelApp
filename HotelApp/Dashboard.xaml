<Page
    x:Class="HotelApp.Dashboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HotelApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Margin="20,0,0,0">
        <TextBlock x:Name="occupiedRoomsTextBlock" FontSize="25"/>
        <TextBlock x:Name="availableRoomsTextBlock" FontSize="25"/>
        <Border BorderThickness="1" BorderBrush="LightGray" Margin="1">
            <StackPanel>
                <TextBlock Text="Today:" FontSize="20"/>
                <TextBlock x:Name="todayCheckIns" FontSize="15"/>
                <TextBlock x:Name="todayCheckOuts" FontSize="15"/>
            </StackPanel>

        </Border>
        <Border BorderThickness="1" BorderBrush="LightGray" Margin="1">
            <StackPanel>
                <TextBlock Text="Tomorrow:" FontSize="20"/>
                <TextBlock x:Name="tomorrowCheckIns" FontSize="15"/>
                <TextBlock x:Name="tomorrowCheckOuts" FontSize="15"/>
            </StackPanel>
            
        </Border>

    </StackPanel>
</Page>
